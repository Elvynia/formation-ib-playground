import { BorderHoverDirective } from './border-hover.directive';

describe('BorderHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderHoverDirective();
    expect(directive).toBeTruthy();
  });
});
